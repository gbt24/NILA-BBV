defaults:
  - dataset: cifar10
  - federated: fedavg
  - model: resnet18
  - progress: default
  - _self_

seed: 0
output_root: outputs/runs
device: auto

hydra:
  run:
    dir: .
  output_subdir: null

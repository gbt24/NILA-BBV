defaults:
  - attack: finetune
  - dataset: cifar10
  - progress: default
  - _self_

checkpoint: outputs/runs/latest/checkpoint.pt
seed: 0
seeds: [0, 1, 2]
studies: [robustness]
output_root: outputs/attacks

hydra:
  run:
    dir: .
  output_subdir: null

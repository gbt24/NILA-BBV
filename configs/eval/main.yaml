defaults:
  - dataset: cifar10
  - owner: owner0
  - verification: margin
  - _self_

seed: 0
seeds: [0, 1, 2]
studies: [main, false_claim]
output_root: outputs/runs

hydra:
  run:
    dir: .
  output_subdir: null

defaults:
  - dataset: cifar10
  - owner: owner0
  - verification: margin
  - progress: default
  - _self_

seed: 0
seeds: [0, 1, 2]
studies: [main, false_claim]
output_root: outputs/runs
device: auto

hydra:
  run:
    dir: .
  output_subdir: null

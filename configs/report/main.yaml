defaults:
  - dataset: cifar10
  - study: main
  - _self_

outputs_dir: outputs/runs
attacks_dir: outputs/attacks
report_root: outputs
bundle_dir: ${report_root}

hydra:
  run:
    dir: .
  output_subdir: null
